# 心灵治愈师
## python虚拟环境
```shell
  mkvirtualenv soul
  workon soul
  pip install -U pip setuptools_scm wheel pbr tox poetry
  CPPFLAGS="-I/usr/local/include -L/usr/local/lib" pip install infra-framework
  pip3 install -U setuptools_scm pip wheel poetry
  pip3 install -U infra-framework
```
